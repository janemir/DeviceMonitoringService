export interface Device {
  id: string;
  name: string;
  version: string;
}

export interface DeviceSession {
  id: string;
  name: string;
  startTime: string;
  endTime: string;
  version: string;
}